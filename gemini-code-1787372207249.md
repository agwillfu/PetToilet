# 🐾 寵物尿盆自動沖水與行為監控系統 (PetToilet Project)

## 1. 專案背景與目標
* **使用者對象**：寵物貓「白白」
* **核心功能**：
  * 利用震動感測器監測貓咪上廁所的行為週期（站上 ➔ 使用中 ➔ 離開）。
  * 貓咪完全離開後自動延遲觸發繼電器/MOSFET 進行自動沖水，避免聲音驚嚇。
  * 沖水期間透過板載 LED 閃爍提供視覺警示，沖水結束自動熄滅。
  * 透過 Blynk IoT 平台進行遠端參數微調（站上門檻、離開門檻、沖水時長）、手動沖水控制與行為日誌/圖表記錄。
* **當前測試階段**：將原本預計的 4 組感測器簡化為 **單組 (D2)** 進行連線、中斷與狀態機邏輯驗證。

---

## 2. 硬體架構與接線規格

### 2.1 核心元件
* **主控板**：NodeMCU (ESP8266)
* **震動感測器**：SW-420 模組（目前測試 1 組，接 D2）
* **水泵驅動**：IRF520 MOSFET 模組（接 D6）
* **電源供應器**：工業級金屬外殼 110V AC 轉 5V DC 開關電源（鎖螺絲型）

### 2.2 接線對照表

#### AC 高壓端接線
| 端子標示 | 接線定義 | 備註 |
| :--- | :--- | :--- |
| **L** | 110V 火線 | AC 輸入 |
| **N** | 110V 中性線 | AC 輸入 |
| **FG / ⏚** | 地線 (Earth) | 金屬外殼接地，防漏電與濾波（重要） |

#### DC 低壓與訊號端接線
| 元件 | 元件接腳 | NodeMCU 接腳 | 說明 |
| :--- | :--- | :--- | :--- |
| **電源供應器** | V+ (+5V) | **Vin** | 供電 5V 給 NodeMCU |
| **電源供應器** | V- (COM) | **GND** | 系統主共地 |
| **SW-420 (震動)** | VCC | **3V3** | 供電 3.3V |
| | GND | **GND** | 共地 |
| | DO | **D2** | 中斷輸入 (RISING 觸發) |
| **IRF520 (MOSFET)** | VCC | **Vin (5V)** | 控制端供電 |
| | GND | **GND** | 控制端共地 |
| | SIG | **D6** | 數位輸出控制 (HIGH: 開啟沖水) |
| | VIN / GND (端子側) | 電源 5V / GND | 水泵外部驅動電源輸入 |
| | V+ / V- (端子側) | 水泵馬達 (+ / -) | 水泵負載輸出 |

---

## 3. 系統邏輯架構（狀態機 State Machine）

系統透過四個狀態進行非阻塞（Non-blocking using `millis()`）運作：

1. **`IDLE` (閒置)**：
   * 監聽 SW-420 震動中斷。若 500ms 內偵測到震動，轉入 `DETECTING`。
2. **`DETECTING` (活動偵測/防誤觸)**：
   * 計時維持超過 `entryThreshold` (預設 5 秒) 持續有震動，判定貓咪就位，轉入 `IN_USE`。
   * 若震動停止超過 3 秒，判定為誤觸，回歸 `IDLE`。
3. **`IN_USE` (使用中)**：
   * 持續監控震動。當無震動時間超過 `exitThreshold` (預設 15 秒)，判定貓咪已離開，觸發 `startFlush()` 轉入 `FLUSHING`。
4. **`FLUSHING` (沖水與警示)**：
   * D6 輸出 HIGH 啟動水泵。
   * 板載 LED 以 500ms 間隔非阻塞閃爍。
   * 計時達 `flushDuration` (預設 10 秒) 後呼叫 `stopFlush()`，關閉水泵與 LED，回歸 `IDLE`。

---

## 4. Blynk IoT 介面配置 (Virtual Pins)

* **V1 (Integer / SuperChart & LED)**：沖水狀態方波 (沖水中: 255 / 閒置: 0)
* **V2 (String / Value Display)**：記錄上次沖水時間戳記
* **V3 (Integer / Button, Push)**：手動強制沖水按鈕
* **V4 (Integer / Slider, 1~30s)**：`entryThreshold` 站上判斷時間
* **V5 (Integer / Slider, 5~60s)**：`exitThreshold` 離開判斷時間
* **V6 (Integer / Slider, 1~30s)**：`flushDuration` 沖水持續時間
* **V10 (String / Labeled Value)**：即時系統狀態文字提示
* **Events**：`pet_entry` (啟用 Notifications 與 History line)

---

## 5. 目前完整實作程式碼 (Arduino / ESP8266)

```cpp
/*
 * Blynk 寵物尿盆自動沖水系統 - 單組感測器 (D2) 測試版
 */

#define BLYNK_TEMPLATE_ID   "你的TEMPLATE_ID"
#define BLYNK_TEMPLATE_NAME "PetToilet"
#define BLYNK_AUTH_TOKEN    "你的AUTH_TOKEN"

#include <ESP8266WiFi.h>
#include <BlynkSimpleEsp8266.h>

// --- 硬體定義 ---
const int sw420Pins[] = {D2}; 
const int numSensors = sizeof(sw420Pins) / sizeof(sw420Pins[0]);
const int irf520Pin = D6;
const int statusLED = LED_BUILTIN; 

// --- 狀態定義 ---
enum State { IDLE, DETECTING, IN_USE, FLUSHING };
State currentState = IDLE;

// --- 參數變數 (可由 App 動態調整) ---
int entryThreshold = 5;  // V4 (秒)
int exitThreshold = 15;  // V5 (秒)
int flushDuration = 10;  // V6 (秒)

// --- 計時與旗標變數 ---
volatile bool vibrationFlag = false;
unsigned long lastVibrationTime = 0;
unsigned long stateStartTime = 0;
unsigned long flushStartTime = 0;
unsigned long lastLEDToggleTime = 0;
bool ledState = false;

// 中斷服務常式
void IRAM_ATTR handleVibration() {
  vibrationFlag = true;
}

// --- Blynk 參數回呼 ---
BLYNK_WRITE(V3) { 
  if (param.asInt() == 1 && currentState == IDLE) {
    startFlush();
  }
}
BLYNK_WRITE(V4) { entryThreshold = param.asInt(); }
BLYNK_WRITE(V5) { exitThreshold = param.asInt(); }
BLYNK_WRITE(V6) { flushDuration = param.asInt(); }

BLYNK_CONNECTED() {
  Blynk.syncVirtual(V4, V5, V6);
}

void setup() {
  Serial.begin(115200);
  Blynk.begin(BLYNK_AUTH_TOKEN, "WiFi名稱", "WiFi密碼");

  for (int i = 0; i < numSensors; i++) {
    pinMode(sw420Pins[i], INPUT);
    attachInterrupt(digitalPinToInterrupt(sw420Pins[i]), handleVibration, RISING);
  }
  
  pinMode(irf520Pin, OUTPUT);
  digitalWrite(irf520Pin, LOW);
  
  pinMode(statusLED, OUTPUT);
  digitalWrite(statusLED, HIGH); // 預設熄滅 (ESP8266 Active-LOW)
  
  Blynk.virtualWrite(V10, "系統就緒 (D2 測試中)");
}

void loop() {
  Blynk.run();
  
  if (vibrationFlag) {
    lastVibrationTime = millis();
    vibrationFlag = false;
  }

  unsigned long now = millis();

  switch (currentState) {
    case IDLE:
      if (now - lastVibrationTime < 500) { 
        stateStartTime = now;
        currentState = DETECTING;
        Blynk.virtualWrite(V10, "偵測到活動...");
      }
      break;

    case DETECTING:
      if (now - stateStartTime >= (entryThreshold * 1000UL)) {
        currentState = IN_USE;
        Blynk.virtualWrite(V10, "白白使用中 🐾");
        Blynk.logEvent("pet_entry", "白白上廁所了");
      }
      if (now - lastVibrationTime > 3000) {
        currentState = IDLE;
        Blynk.virtualWrite(V10, "等待中");
      }
      break;

    case IN_USE:
      if (now - lastVibrationTime >= (exitThreshold * 1000UL)) {
        startFlush();
      }
      break;

    case FLUSHING:
      // 非阻塞 LED 閃爍 (每 500ms 切換)
      if (now - lastLEDToggleTime >= 500) {
        ledState = !ledState;
        digitalWrite(statusLED, ledState ? LOW : HIGH);
        lastLEDToggleTime = now;
      }

      if (now - flushStartTime >= (flushDuration * 1000UL)) {
        stopFlush();
      }
      break;
  }
}

void startFlush() {
  Serial.println("開始沖水...");
  currentState = FLUSHING;
  flushStartTime = millis();
  lastLEDToggleTime = millis();
  digitalWrite(irf520Pin, HIGH);
  
  Blynk.virtualWrite(V10, "正在沖水...");
  Blynk.virtualWrite(V1, 255); 
}

void stopFlush() {
  Serial.println("沖水結束");
  digitalWrite(irf520Pin, LOW);
  
  digitalWrite(statusLED, HIGH); // 關閉 LED
  ledState = false;

  currentState = IDLE;
  Blynk.virtualWrite(V10, "等待中");
  Blynk.virtualWrite(V2, String(__TIME__)); 
  Blynk.virtualWrite(V1, 0); 
  Blynk.logEvent("flush_complete", "清理完畢！");
}